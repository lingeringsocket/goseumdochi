include "artificial"

akka {
  loglevel = "WARNING"
}

goseumdochi {
  orientation {
    persistence-file = "/dev/null"
    centering-undershoot-factor = 1.0
  }
  control {
    monitor-visibility = false
  }
  test {
    active = true
  }
}

